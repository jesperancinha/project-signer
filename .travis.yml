jobs:
  include:
    - language: java
      stage: Project Signer Root (Java)
      jdk:
        - openjdk11
      install:
        - mvn clean install
      script:
        - mvn install coveralls:report -Dconsolerizer.show=false -DrepoToken=${COVERALLS_TOKEN}
        - bash <(curl -s https://codecov.io/bash)
